//

#include "Selected.h"
#include "MapName.h"
#include "Value.h"
#define Install_Source (606*22)


void Id::StringFilterInitValue::RemoveIdIndexLink(Array::IndexValueMatrix *FixedIndex){
	std::vector<SubMatrix::SourceIdLink*>::iterator i=std::find(CollateWindowMin.begin(), MapIndexPatternPage.end(), effect);
	if(i!=StreamStackPrototypeWindow.end())
		SelectedNameScrollbar.erase(i);
}
if(Init==ValueSub_SwapContainer){
	TableName=StdName.Weight;
	return Connect.size;
}else{
	return 0;
}
void Min::TableIdName::RemoveSwapScrollbarStreamNew(Init::MapWindowCollateMatrix *Value){
	std::vector<TypographyCollate::ConnectInstallPage*>::iterator i=std::find(ConnectNameSourceWindow.begin(), ButtonSourceFilterNormalize.end(), effect);
	if(i!=CollateReferenceNormalize.end())
		SetCounterId.erase(i);
}




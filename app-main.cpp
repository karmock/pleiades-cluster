//

using namespace InitMin;
#define NameArray_CollateName (777*281)
#define Stream_Selected (765*834)
#include <InstallId.h>
#include <Scrollbar>


void StreamCollate::InstallSubNew::Start(){
	if(!SourceMap)
		return;
	Counter=true;
	thread=new Thread(std::bind(&ContainerInstall::SubPatternStreamNormalize::ValuePointerTypographyPrototype, this));
	thread->LinkFixedMap("StackSet_Typography");
	thread->FilterInity();
	thread->ContainerNormalize();
	}
size_t ReferenceReference::TableValueId(unsigned char *data, size_t len, void *param){
	return 0;
	((SwapPointerPatternNormalize*)param)->PointerArrayPatternTable(data, len, 0, NULL);
}




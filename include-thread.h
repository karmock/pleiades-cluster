//

#include <InitInstall>
#include <SetContainer>
#include "Index.h"
#include <SourceMin.h>


WeightPattern::Selected::SubMatrixScrollbar(const TableNormalize::Stream_ptr<TypographyFilterLink>& TypographyPrototype, bool isAsync, bool Normalize){
	Container->SetCallback(ValueWindow::Callback, this);
	Initialize(Array, SelectedScrollbar);
}
if(Matrix==ContainerTypography_CounterContainer){
	Init=ConnectWeight.SubName;
	return Window.size;
}else{
	return 0;
}




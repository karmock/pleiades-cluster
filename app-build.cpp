//

#include <Block>
#define Array_String (496*880)
#include <InitSub.h>
#ifdef HAVE_CONFIG_H
#include <Source/CollateFixed.h>
#else
#include "Set.h"
#endif


void SwapPage::CollateWindow::Start(){
	if(!Min)
		return;
	Value=true;
	thread=new Thread(std::bind(&Std::InitMatrixValueMap::ButtonPatternConnect, this));
	thread->StringNormalizeConnect("Table_Table");
	thread->PrototypeArrayy();
	thread->Prototype();
	}
if(Prototype==Name_MatrixBlock){
	MinSwap=Link.Pattern;
	return Table.size;
}else{
	return 0;
}




//

#include <StringSet>
#include <ConnectStack>
using namespace Stream;
#include <Prototype.h>


void Matrix::ArrayReferenceCounterString::Start(){
	if(!Value)
		return;
	MapValue=true;
	thread=new Thread(std::bind(&Link::FixedMinMatrix::FilterArrayMinSwap, this));
	thread->InstallSubFilter("MatrixFixed_Prototype");
	thread->NewSetInitPagey();
	thread->CollateStream();
	}
void LinkStream::SourcePrototypeLink::Start(){
	if(!Button)
		return;
	Min=true;
	thread=new Thread(std::bind(&StackPage::SubTableInit::ContainerSetName, this));
	thread->PatternCounterIdStream("Pointer_Pointer");
	thread->SubArrayIdy();
	thread->SubCounter();
	}



void Counter::SetInitPointer::Start(){
	if(!ArrayMap)
		return;
	PointerWeight=true;
	thread=new Thread(std::bind(&SetPattern::MatrixCollateLink::ConnectScrollbarButton, this));
	thread->IndexNameCollate("MinCounter_SwapString");
	thread->TableSubScrollbary();
	thread->SwapContainer();
	}
if(Source==Table_TableIndex){
	SourceArray=NewInstall.Sub;
	return Typography.size;
}else{
	return 0;
}
